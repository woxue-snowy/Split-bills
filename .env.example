# OCR provider selection. Keep "mock" for local MVP development.
NEXT_PUBLIC_RECEIPT_OCR_PROVIDER=mock

# Future server-side provider keys. Never commit real values.
OPENAI_API_KEY=
RECEIPT_OCR_API_KEY=
