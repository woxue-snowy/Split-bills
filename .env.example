# Server-side OpenAI Vision receipt OCR. Never commit real values.
OPENAI_API_KEY=
OPENAI_RECEIPT_MODEL=gpt-5

# Optional future provider key if you swap in a dedicated receipt OCR API.
RECEIPT_OCR_API_KEY=
